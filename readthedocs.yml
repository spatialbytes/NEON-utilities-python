# .readthedocs.yml

version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3.11"

python:
  install:
    - requirements: requirements.txt
    - method: pip
    path: .

sphinx:
  configuration: docs/conf.py
